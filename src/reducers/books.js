import React from 'react';

const bookReducer = (defaultState = state, action) => {
	switch(action.type) {
		case 'CREATE_BOOK':
			break;
		case 'REMOVE_BOOK':
			break;
	}
	return state
}

export default bookReducer;
